a) it is easy to restore the previous version of a program if something in the current version is unstable.
b) when a large enough change is made so that it is easy to check which addition to the code made something go wrong.
c) they explain what was changed in a version of the program so it is easier to understand the changes.
